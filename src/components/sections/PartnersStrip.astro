---
import { partners } from "../../data/partners.ts";
---

<div class="partners">
  <span class="partners-label">Trusted Partners</span>
  <div class="partners-logos">
    {partners.map(partner => (
      <span class="partner-logo" set:html={partner.labelHTML} />
    ))}
  </div>
</div>